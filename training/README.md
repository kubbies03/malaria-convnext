# Training Assets

This folder contains the training-side notebook retained as project reference material.

- `ConvNeXt.ipynb`: notebook used during the model experimentation and training workflow

The main deployment review path starts in `deploy/` and `docs/`, while this folder provides supporting context for how the model work originated.
